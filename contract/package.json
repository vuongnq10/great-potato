{
  "name": "potato-smart-contract",
  "version": "1.0.0",
  "description": "This is contract for potato project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat --network sepolia run scripts/deploy.js",
    "mint": "node scripts/mint.js",
    "copy-json": "node -e \"const fs = require('fs'); const data = fs.readFileSync('./artifacts/contracts/Contract.sol/ERC721NFT.json'); fs.writeFileSync('../src/content/NFT/content/ERC721NFT.json', data);\"",
    "run": "yarn compile && yarn deploy && yarn mint && yarn copy-json"
  },
  "author": "Vuong Nguyen",
  "license": "MIT",
  "devDependencies": {
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@alch/alchemy-web3": "1.4.7",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@openzeppelin/contracts": "4.9.3",
    "alchemy-sdk": "^2.10.1",
    "dotenv": "^16.3.1",
    "ethers": "5.7.2",
    "fs": "^0.0.1-security"
  }
}